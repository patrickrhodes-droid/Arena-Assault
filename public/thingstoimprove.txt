-- IMPLEMENTED --
- When you revive you should spawn with full health (done)
- Health packs should restore 150 health points (done)
- Fix stale enemy reference in soldier bullet setTimeout (done)
- Boss impervious alert now has a 2.5s cooldown (done)
- announceWave null-guards game.dom.waveAnnounce (done)
- bulletHit handler validates weapon string and clamps damage to 10000 (done)
- Deleted dead _removedHostAI code stub from enemies.js (done)
- Skeleton spawn count and speed reduced; hitbox enlarged (done)
- Bots run Three.js AI on host client — wall collision, LOS, hit detection (done)
- Minimap fixed: 450px draw into 360px canvas (done)
- Damage dealt stat tracks actual HP removed for all weapons (done)
- Pause freezes bots when all alive players are paused (done)
- Skeleton groups no longer spawn stacked (done)
- fromRemote enemy bullets skip all damage checks (done)
- Skeleton animation mixers culled at 30 units (done)
- Object.values in handleRevive hot path (done)
- Particle system capped at 200 (done)
- HUD shows correct wave immediately on match start (done)
- PvP respawn camera snaps to spawn position (done)
- Sprint speed reduced 50%; sword and sniper deal 500 dmg; sword deals 250 vs boss (done)
- Boss knockback restored through server (done)
- Boss jump works in solo play (>= 1 nearby player, was >= 2) (done)
- Bullet timing fixed: each client runs its own hit detection for enemy bullets (done)
- Item 10: Shared constants extracted to gameConstants.js; config.js re-exports from it (done)
- Item 18: PvP respawn camera snapped on respawn (done)
- Item 20: Basic reconnection — name-based state restore within 30 s of disconnect (done)
- Grappling hook added (G key): raycast to attach point, pulls player at 18 u/s;
  jump to release mid-swing; lure + rope visuals (done)

-- IMPLEMENTED (continued) --
- Boss attack now lands reliably: boss continues pursuing at half-speed during wind-up,
  and the dist<7.8 re-check at swing time was removed (wind-up entry already validated range).
- Canvas textures added to all three maps: metal-panel walls (Arena), sandstone walls/structures
  (Desert), asphalt ground + concrete-brick buildings/walls (City).
- Props from City Props and shooter asset packs added to all maps with AABB collision:
  Arena — barrels, gas tanks, water tanks, pallets, tires, crates, sack trenches, dumpsters.
  Desert — barrels, gas cans, tires, crates, sack trenches, pallets, broken cars, tank, trash containers.
  City — street lights, trees, trash cans, fire hydrants, bollards, benches, traffic cones,
          traffic lights, stop signs, manhole covers, trash bags, bike racks, concrete barriers,
          box piles, bushes, fallen leaves, dumpsters, broken cars, shipping containers, signs.
- Revive bug fixed: `id` was undefined in handleRevive (now uses remotePlayer.playerId).
- Enemy pathfinding fixed: segmentIntersectsExpandedBox and getDetourDirection used
  obs.xMin/xMax/zMin/zMax but obstacles store obs.min.x/max.x/min.z/max.z — now correct.
  Soldiers, dogs, and skeletons will now route around obstacles instead of getting stuck.
- Boss escape no longer fires ghost hits: windupTmr and swingTmr are cleared when the
  boss starts a jump-escape, preventing a hit registering after the boss has teleported.
- Ladder shooting fixed: bulletHitObstacle now uses y < obstacle.h (exclusive) instead of
  y <= obstacle.h so bullets fired from the exact top surface of a structure aren't blocked.
- Config constants added: PLAYER_MOVEMENT.crouchScale (0.65), PLAYER_MOVEMENT.recoilDecay (0.85),
  PLAYER_MOVEMENT.crouchLerp (14), SOLDIER_TUNING.bulletSpreadH (0.24). All wired into code.
- City map preview card updated to sunny gradient (was still showing old dark nighttime colors).
- Controls list in lobby updated: added grapple (G), slot 6, double-tap W sprint, ladder, LMB.
- Kill feed added (top-right HUD): shows enemy kills with score, boss kills in gold, wave clears.
- Wave-clear banner added: "WAVE X CLEARED" animated banner appears when a wave ends.
- Sprint and Crouch status pills added to HUD (above minimap, light up when active).
- Grapple cooldown bar added to HUD: shows fill % as cooldown runs down, green ✓ when ready.
- Minimap now shows health packs as green crosses (animated bob).
- "Give Up" button added to the downed overlay — immediately enters spectator mode.
- Dead reviveProgress object removed from game state (was never read, just noise).
- Duplicate HP assignments removed in startGame / startPvPGame.

-- IMPLEMENTED (continued) --
- Server converted to ESM (import/export): all require() → import, __dirname fixed via
  fileURLToPath, package.json gets "type":"module". fs, path, os all destructure-imported.
- Reconnection token: client generates a UUID stored in localStorage (survives refreshes).
  Server keys recentlyDisconnected on the token instead of playerName; TTL extended to 60 s.
  New `registerToken` socket event stamps the token onto the server-side player entry on connect.
- Persistent leaderboard: server reads/writes leaderboard.json (top 20 COOP + PvP entries).
  Recorded on every COOP game-over and PvP match-over. Exposed at GET /api/leaderboard.
  Game-over screen has "ALL-TIME LEADERBOARD" button that fetches and displays the table.
- Room password: host can optionally set a password in the Host Controls panel. Non-host
  clients who join a locked room see a password prompt overlay before entering the lobby.
  Server validates via `submitPassword` event and disconnects wrong-password clients.
- Destructible props: explosive barrels and gas cans/tanks explode when shot — large particle
  burst, 7-unit AOE enemy damage (200 at centre, falls off linearly), collision entry deactivated,
  mesh removed. Host relays `propDestroyed` event so all clients see the destruction.
- Boss second phase: at 50% HP the boss enters an enraged state — speed rises from 12 to
  ~18.6 units/s, attack cooldown drops from 1.1 s to 0.65 s, and the body meshes gain an
  orange-red emissive glow as a visual warning to players.
- Player floating fix: all player body-part Y positions lowered by 0.06 units (both local
  player and remote players) so boot bottoms overlap the ground plane, eliminating the
  visible floating gap in third-person view.
- Blacksite lighting overhauled: hemisphere intensity raised to 1.4, fog density halved,
  fluorescent point lights multiplied and their radius/intensity roughly doubled, so the
  facility interior is fully visible while retaining the dark tactical atmosphere.
- Bazooka scale tripled (0.13 → 0.38) and first-person position pulled closer to camera so
  the weapon fills the screen correctly instead of appearing tiny and far away.
- Map preview in lobby is now live: selecting a map card instantly rebuilds the 3D scene
  behind the UI to show the actual map geometry as a real-time preview.

-- IMPLEMENTED (continued) --
- Wave-6 freeze fixed: enemy soldier bullets referenced undefined `previousPosition` in
  updateBullets (should have been `bullet.prevPos`). Since soldiers only appear from wave 6,
  this caused a ReferenceError that silently killed the requestAnimationFrame loop exactly at
  that wave. Fixed on all three usages in combat.js.
- Crosshair hit flash: #crosshair-dot plays a white→orange scale animation (0.18 s) each
  time processHit() fires. CSS `hit` class added/removed with forced reflow to restart it.
- Color-coded death particles: enemyKilled handler now picks particle color by type —
  white (skeleton), dark red (soldier), orange (dog), gold (boss). Boss also gets 40 big
  particles instead of 18 for a more dramatic death.
- Wave enemy progress bar: thin orange bar + "N LEFT" count appear below the wave number
  during SPAWNING/ACTIVE states. Tracks enemies spawned via enemySpawned events and fills
  as enemies are killed. Resets on each new wave.
- Score pop-ups: "+100" floats up from the crosshair area on each killCredit event.
  Color-coded: white (skeleton/soldier), orange (.dog), gold (.boss). Auto-removed after 1.2 s.
- Damage direction indicator: red gradient arc at screen edge that rotates to point toward
  the damage source. Triggers on both bullet (playerDamaged, looks up enemy by shooterId)
  and melee (enemyMeleeAttempt, uses ex/ez). CSS animation fades over 1.4 s.
- Sprint FOV boost: +8° FOV added to target when sprinting forward (not when aiming).
  Smoothly lerped at the same rate as ADS FOV transitions.
- FPS head-bob: sinusoidal vertical camera offset (amplitude 0.038 walk / 0.055 sprint)
  applied in updateCamera when first-person, moving, grounded, and not crouching.
  Uses game.walkTime which already tracks walk cycle frequency.
- Boss phase label: "#boss-phase-label" element added below boss HP bar. Shows "PHASE 1"
  (amber) or "PHASE 2 — ENRAGED" (red) based on boss.bossPhase2 flag. Updates in updateHUD.
- Coyote time: 80 ms jump grace window after walking off a ledge. wasGrounded saved at the
  start of updatePlayer; coyoteTmr set to 0.08 when transitioning grounded→airborne while
  falling. tryJump allows jump if coyoteTmr > 0 and consumes it immediately.
- Muzzle flash 3× bigger: SphereGeometry radius 0.1 → 0.32, PointLight range 5 → 9 and
  intensity 3 → 8, muzzleTmr duration 0.04 → 0.10 s.

-- TO DO --

10. (residual) server.js still duplicates some constants from gameConstants.js.
    Since the server is now ESM it can directly import them — this is a remaining cleanup task.

20. (residual) Reconnection uses playerName as identity key which is not
    unique. A proper fix requires a persistent session token (cookie/localStorage).



-- KNOWN RELIABILITY ISSUES --

tickEnemies (server.js) is never called from startGameLoop. The server-side boss AI
  (attack, movement, escape jump) is dead code. All enemy AI runs on the host client.
  This is intentional per the architecture but means if the host disconnects mid-wave,
  all enemies freeze. Consider adding a note in server.js or eventually calling
  tickEnemies as a fallback when no client owns an enemy.

enemyMeleeAttempt server handler does not verify the sender is the host/owner of the
  enemy. Any connected client could theoretically send fake boss hits to damage other
  players. Low risk for LAN play but worth noting.

Boss escape jump now clears windupTmr and swingTmr on launch — ghost hits fixed.
  Remaining concern: boss atkTmr is not reset, so the first attack after landing
  may fire immediately if atkTmr had already expired before the jump.

some ladders dont have an associated model (city and desert)
after having selected map change background map preview to selected map
when on floor player looks like hes floating and his shadow is to the left (3rd person)
desert and city look very similar now colours wise

-- PERFORMANCE IMPROVEMENTS IMPLEMENTED --
- Shadow map: PCFSoftShadowMap → PCFShadowMap (4-tap instead of 9-tap; same look at
  play distance, meaningful GPU cost reduction).
- Shadow map resolution: 2048×2048 → 1024×1024 (4× fewer shadow pixels to compute).
- HUD + status indicators: updated every 2nd frame instead of every frame.
- Minimap: redrawn every 3rd frame instead of every frame (canvas 2D ops are expensive).
- Bullet loop: eliminated per-bullet position.clone() each frame; previous position is
  now stored directly on the bullet. Eliminated per-bullet×per-enemy Vector3 allocation
  by reusing module-level _enemyCenterVec/_hitPosVec cached vectors.

-- PERFORMANCE IMPROVEMENTS NEEDING COMPROMISE OR LARGER EFFORT --
- Geometry instancing: identical props (barrels, crates, bollards) are separate draw
  calls. THREE.InstancedMesh would reduce draw calls by 30-50% on prop-heavy maps.
  Requires refactoring loadProp to collect instances before rendering.
- Shared material caching: each map creates new MeshStandardMaterial objects; caching
  across rebuilds would cut shader compilation pauses on map change.
- Occlusion culling: Three.js has no per-object occlusion culling. For Blacksite with
  its many interior walls, a BVH-based system (three-mesh-bvh) could skip rendering
  objects in other corridors. Significant third-party integration needed.
- Web Workers for AI: enemy AI runs on the main thread and spikes on large waves.
  Moving to a Worker would keep the render thread smooth but requires a complex state-
  sharing refactor.
- Texture atlassing: each GLB prop loads its own textures; atlassing would reduce GPU
  texture-swap overhead but needs an asset pipeline change.
- FXAA instead of MSAA: the renderer uses antialias:true (MSAA), which is expensive.
  Switching to FXAA postprocessing would be faster but softer-looking at edges.

-- IDEAS NEEDING EXTERNAL CHANGES OR LARGER EFFORT --

Sound effects (currently just music + basic tones):
  - Footstep audio per surface type (metal, sand, concrete)
  - Reload "click" per weapon (brass, magazine, pump)
  - Bullet whiz-by on near miss
  - Boss roar / growl when it charges
  - Ambient wind for desert, urban noise for city
  - Wave-clear victory sting
  Needs: real audio files (WAV/OGG) per weapon/surface.


Character cosmetics / skins:
  Currently all characters share the same body mesh; adding per-character
  torso textures/models would require GLB files and a selection system.

Persistent leaderboard:
  Scores currently reset when the server restarts. A SQLite/JSON file or
  small DB would let you track all-time high scores and wave records.

Lobby password / private rooms:
  Anyone with the IP can join. A room code system would let you host
  private LAN sessions with more control.

More PvP modes:
  - Team deathmatch (2v2 or 1v1v1v1 with team allocation)

Destructible props:
  Some props (barrels, gas cans) could explode when shot, dealing
  area damage. Requires server-side prop state and a destroy event.

Map voting / rotation:
  Instead of host choosing, a vote at end of match that auto-rotates
  to the most-voted map. Small UX improvement with big feel payoff.

Boss second phase:
  At 50% HP the boss could speed up, gain a ranged attack (thrown rock),
  or split into two smaller brutes. Adds drama to the fight.

Ladder GLB models for city and desert:
  Currently the fire escapes in City use box geometry staircases; the
  pyramid in Desert has no visible ladder. Adding GLB ladder props
  matching the ladder zone positions would make climbing more readable.

Shadow / floating fix (3rd-person):
  Player model hovers slightly above ground and shadow is offset left.
  Needs investigation of playerGroup Y offset and shadow camera alignment.

Server ESM conversion:
  server.js is CommonJS so it cannot import shared constants from
  gameConstants.js directly. Convert to ESM or add an esbuild step to
  eliminate the duplicated constant definitions.

Reconnection token:
  Current reconnection uses playerName as identity, which isn't unique.
  A proper fix needs a session token stored in localStorage/cookie.